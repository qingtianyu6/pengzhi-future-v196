# 棚智未来 v1.3.0 — 下一层精修报告

本轮在上一版“纯图片/UI 分离 + 全站字体间距统一 + 核心交互落地”的基础上，继续完成 **移动端产品化、交互细节、性能与可访问性** 四个方向的深化。

## 1. 移动端不再只是桌面版缩小

### 全站移动导航
- 新增真正的 Hamburger 菜单与右侧抽屉导航。
- 抽屉包含：首页、平台介绍、核心功能、技术亮点、应用场景、项目成果。
- 游客模式 / 登录 / 注册在移动端完整保留。
- 抽屉内可直接打开站内搜索。
- 支持 ESC 关闭、点击遮罩关闭、路由切换自动关闭。
- 弹层打开时锁定 body 滚动，避免背景误滚动。

### 首页
- 430px 下重新设计主标题、按钮、四项能力区和主视觉顺序。
- 双按钮在窄屏自动纵向排列。
- 功能入口改成 2×2 轻量信息网格。
- 保留一张实时数据卡，避免移动端完全失去“智能平台”动态感。

### 平台介绍
- Hero、痛点链、平台能力、数据到行动、真实场景全部重新适配移动端。
- “为什么做”在手机上改成可横向滑动的真实场景卡，减少页面过长。
- “平台能做什么”使用纯温室照片 + HTML 热点卡，移动端改成可读的纵向排列。
- “从数据到行动”由横向曲线转换成移动端纵向时间线。
- “走进真实的大棚”改成横向滑动场景组。

### 核心功能
- 五大能力在移动端改成横向滑动信息条。
- 环境指标支持窄屏横向浏览，不挤压字号。
- 病害识别三个区域改为自然单列。
- 农事操作入口改为 2 列触控友好布局。
- 数据沉淀改为 2×2。
- 页尾 CTA 在手机端重新恢复并收紧比例。

### 技术亮点
- 多源数据、边缘能力使用 2×2 布局。
- 知识图谱重新规划移动端节点位置，避免重叠。
- 算法迭代流程在手机端转换成 2×2 流程块。
- 技术照片、建议列表、分层架构按单列阅读顺序组织。

### 应用场景
- 每个场景采用“照片 → 横向滑动能力卡 → 收益”的手机阅读顺序。
- 三个能力卡不再强行压成一列长页面，而是滑动浏览。
- 区域地图与基地信息重新组织为单列。

### 项目成果
- Hero 成果指标在移动端转换为 2×2 数据块。
- 技术成果在窄屏自动降为单列。
- 应用成效数据改成触控友好的指标块。
- 荣誉证书在手机端改成横向滑动，不再把证书压得过小。

## 2. 交互体验深化

- 全站链接、按钮、输入框、选择框补充 `:focus-visible` 可见焦点。
- 主导航与移动导航增加 `aria-current="page"`。
- 搜索、导航支持 ESC 关闭。
- 路由切换自动滚动到页面顶部。
- 搜索/移动菜单打开时自动锁定页面背景滚动。
- 增加 `prefers-reduced-motion` 支持，减少动画用户不会被强制播放动效。

## 3. 性能与稳定性

- 首屏 Hero 图片使用 eager 加载。
- 非首屏图片统一使用 `loading="lazy"` + `decoding="async"`。
- 长页面 Section 在支持的浏览器中启用 `content-visibility:auto`。
- 为延迟渲染区域设置 intrinsic size，减少滚动时布局跳变。
- 保留现有 ECharts、上传识别、农事新增、CSV 导出、智能建议等真实交互。

## 4. 修复上一层遗留的一处重要问题

上一版在将“平台能做什么”由设计截图替换成纯高清温室照片后，旧 CSS 仍保留了“隐藏标题/热点”的规则。本轮已彻底修正：

- 桌面端重新显示真实 HTML 标题与 4 个功能热点。
- 移动端重新排版热点卡。
- 不再依赖图片内的文字或 UI。

## 5. QA

已对营销站六页做以下离线渲染检查：

- 430 × 932 手机视口
- 1440 × 900 桌面视口

预览文件位于：

`visual_qa/next_layer/`

其中：
- `mobile_430_overview.png`
- `desktop_1440_overview.png`
- 六张页面手机长截图

注意：当前容器无法从 npm Registry 补全全部依赖，因此这些 QA 图主要用于 CSS/布局复核；项目源码仍保持 React/Vite 正式结构。

## 6. 本轮代码检查

- 57 个 TS / TSX 文件使用 TypeScript `transpileModule` 做语法检查：0 错误。
- 后端 Python `compileall`：通过。
- 版本号：`1.3.0`

