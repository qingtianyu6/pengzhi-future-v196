# 棚智未来 v1.6.0 — 智能温室总览 1:1 复刻说明

本轮以 `智能温室管理总览.png`（1586×992）为唯一视觉基准，对管理系统外壳与“种植总览”页面进行像素级重构，同时保留原有 API、路由、模型中心、任务跳转和大棚工作区切换能力。

## 本轮核心修改

1. **系统外壳**
   - 侧栏展开宽度调整为 254px，颜色由过深墨绿调整为更接近参考稿的深浅渐变绿。
   - 品牌区、当前大棚切换器、导航分组、底部系统状态和管理员区域重新按参考图比例排版。
   - 顶部栏按参考图重构：面包屑、更新时间、系统状态、通知角标、头像和下拉箭头。

2. **种植总览**
   - 顶部改为“问候 + 大棚状态 + 生长标签 + 天气 + 模型中心”的横向 Hero。
   - 5 个环境指标卡采用不同低饱和状态色、状态胶囊和微型趋势线。
   - “过去 24 小时环境趋势”与“今日待办”改为参考图的 2:1 主次布局。
   - 环境趋势图新增温度、空气湿度、土壤湿度 3 条曲线，并统一 Tooltip、图例和网格风格。
   - 风险、数据健康、决策状态改为 3 张轻量信息卡。
   - 最近动态改为紧凑行式信息流，不再留大片空白。

3. **功能保持**
   - 大棚工作区仍从真实 API 获取并可切换。
   - 总览数据仍从 `getDashboardSummary` 获取。
   - 模型中心按钮保持可用。
   - 待办、环境趋势、农事任务跳转保持原有路由逻辑。
   - 系统服务状态仍使用后端健康检查。

## 视觉 QA

- 参考尺寸：1586×992
- 静态复刻预览：`visual_qa/dashboard_v16/dashboard_actual_1586x992.png`
- 50% Overlay：`visual_qa/dashboard_v16/dashboard_overlay_50pct.png`
- Difference：`visual_qa/dashboard_v16/dashboard_difference.png`

说明：当前运行容器没有完整 `node_modules`，因此不能在此环境严谨声明 `npm run build` 已通过。已执行 TypeScript `transpileModule` 语法检查与后端 Python `compileall`。在正常联网环境执行 `npm install && npm run dev` 即可运行完整 React/Vite 项目。
