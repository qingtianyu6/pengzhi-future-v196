# 棚智未来 v1.9.1 全站高清素材替换完成报告

## 本轮完成内容

本轮针对 v1.9 清晰度终验中标记的 7 张问题素材完成替换，并同步消除会造成浏览器位图重采样的照片缩放动画。

### 已替换素材

1. 平台介绍：传感器设备 → 使用新版高清传感器实景
2. 平台介绍：手机信息 → 重绘为高清温室背景 + 清晰手机数据界面
3. 平台介绍：病害叶片 → 重绘为高清病斑叶片视觉
4. 平台介绍：农事记录 → 重绘为高清农事记录本视觉
5. 平台介绍：温室数据场景 → 使用新版高清温室实景
6. 平台介绍：团队实地测试 → 使用高清温室实景 + 团队测试视觉
7. 应用场景：规模化基地 → 淘汰旧软糊航拍图，替换为高细节规模化温室种植场景

### 清晰度相关代码修复

- 首页大图取消 `scale(1) -> scale(1.012)` 漂移动画，避免持续亚像素重采样。
- 平台介绍 Hero 图片取消位图上下浮动动画。
- “走进真实的大棚”卡片取消图片 hover scale，改为保持原生像素映射。
- 文字、卡片、图标仍由 React / CSS 渲染，不烘焙进大面积背景照片。

## 验收结果

- 7 张目标素材均已替换为更高细节版本。
- 平台介绍旧 `photo_refs` 文件名保持不变，因此原页面逻辑无需修改即可读取新素材。
- `regional-base-clean.png` 已替换。
- 前端相对资源引用检查通过。
- TypeScript / TSX 语法解析检查通过（未发现 parse error）。
- 后端 Python `compileall` 通过。
- ZIP 完整性检查通过。

## 说明

本轮重点是“消除糊图源头”，不是继续对低清源图做无意义放大。部分平台介绍小图采用高清实景背景与矢量化信息主体的组合方式，保证在 2K / Retina 屏幕上依旧清晰。
