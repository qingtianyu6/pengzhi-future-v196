# 棚智未来 v1.9 高清实景素材重构报告

本轮目标不是“把旧图放大”，而是替换原始低细节素材并停止浏览器二次缩放。

## 已替换的关键运行素材

- `greenhouse-wide.png`：新的纯温室实景底图，1680×936，不包含文字 / UI / Logo。
- `sensor-clean.png`：新的高清温室传感器实景图，1536×1024，不包含文字 / UI / Logo。
- `greenhouse-card.png`：新的真实温室工作人员 + 平板场景，1680×936，不包含文字 / UI / Logo。
- 同目录生成 `.webp` 高质量伴随版本，便于后续 CDN / picture / srcSet 使用。

这三张素材覆盖了首页、平台介绍、核心功能、技术亮点、项目成果等页面中最频繁复用、最容易被放大看糊的视觉位置。

## CSS 清晰度修复

1. 移除 `MarketingShared.css` 中 Hero 图片的 `scale(1.015) → scale(1.04)` 呼吸缩放。
2. 移除 Footer Banner 的 `scale(1.035)` 二次重采样。
3. 核心功能页关键照片强制 `transform:none` / `filter:none` / `image-rendering:auto`。
4. 动效继续由 DOM、卡片、图表、遮罩承担，不再持续缩放摄影图片。

## 为什么会更清楚

旧素材虽然像素尺寸较大，但本身经过强锐化/平滑，真实细节已经丢失；浏览器再进行 transform scale 会继续重采样。新方案使用重新生成的纯摄影素材，并保持照片原生像素映射。

## 建议浏览器验收

- 浏览器缩放：100%、125%
- 桌面宽度：1920 / 1600 / 1440 / 1366
- 重点检查：核心功能 Hero、环境监测设备、大棚管理背景、平台介绍实景图
- 打开 DevTools 确认没有 CSS transform 持续作用在摄影图片上
