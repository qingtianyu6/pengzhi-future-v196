# 棚智未来 v1.9.3 — 第一参考图统一首屏风格

本版以用户确认的“第一张核心功能参考图”为统一视觉目标。

## 核心变化

- 核心功能页从整屏 `100vh` Hero 调整为约 540–592px 的沉浸式大图 Banner，让下一节内容在首屏下方自然露出。
- 保留“整张高清温室照片铺满 Hero + 左侧白色渐变遮罩 + 左侧文案 + 右侧真实 DOM 平台界面”的结构。
- 左侧标题、说明、五项能力与 CTA 全部缩回参考图比例，避免第二版中过大的标题和过重的前景叶片。
- Dashboard 尺寸缩小并上移，阴影减轻，仅保留极轻的 DOM 浮动；背景照片固定，不做 scale/呼吸动画。
- 平台介绍、技术亮点、应用场景、项目成果四页统一采用同样的 520–575px 摄影 Hero：照片全宽铺底，左侧文案压图，右侧保留各页自己的实时 DOM / 数据卡 / 指标。
- 平台介绍 Hero 的温室图、应用场景 Hero、项目成果 Hero 接入 1920 / 2560 / 3360 WebP `srcSet`，浏览器按屏幕自动选择清晰版本。
- 移动端仍保留现有单列响应式逻辑，不强制使用桌面的压图布局。

## 设计原则

1. 图片负责真实农业场景；文字、数据、Dashboard、指标全部由 HTML / React / SVG / ECharts 渲染。
2. Hero 不再占满一整屏，首屏需要看见下一模块的开头，形成第一参考图的页面节奏。
3. 摄影图片禁止 transform scale 动画，优先保证锐度。
4. 各营销页共享同一套遮罩、标题比例、Hero 高度与视觉节奏，但保留页面自身的信息表达。
